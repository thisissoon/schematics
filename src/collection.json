{
  "$schema": "../node_modules/@angular-devkit/schematics/collection-schema.json",
  "schematics": {
    "testing": {
      "description": "A schematic to set up CI and testing patterns",
      "factory": "./testing/index",
      "schema": "./testing/schema.json"
    },
    "universal-express": {
      "description": "A schematic to set up angular universal and an express server",
      "factory": "./universal-express/index"
    },
    "docker": {
      "description": "A schematic to build docker image",
      "factory": "./docker/index",
      "schema": "./docker/schema.json"
    }
  }
}
