#
# CircleCI configuration for <%= name %>
#

version: 2
jobs:
  build:
    working_directory: ~/<%= name %>
    docker:
      - image: circleci/node:8-browsers
    steps:
      - checkout
      - restore_cache:
          key: ~/<%= name %>-{{ .Branch }}-{{ checksum "package.json" }}
      - run: npm install
      - save_cache:
          key: ~/<%= name %>-{{ .Branch }}-{{ checksum "package.json" }}
          paths:
            - "node_modules"
      - run: npm run lint
      - run: xvfb-run -a npm run test -- --no-progress --code-coverage
      - run: xvfb-run -a npm run e2e -- --no-progress
      - run: npm run coverage
